import type { Context } from '../../core/context';

async function addFlaggedPhrase(_phrase: string, _context: Context) {
    // TODO: Persist phrase to preferences store when implemented
    return null;
}

export default addFlaggedPhrase;
